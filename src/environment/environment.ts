export const environment = {
     production: false,
     envName: "prod",
     apiBaseUrl: "http://localhost:3001/api",
     remark:"can add other env files according to requirement and update env setup in angular.json"
}